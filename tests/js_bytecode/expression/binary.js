function test1(x, y) {
  x + y;
  x - y;
  x * y;
  x / y;
  x % y;
  x ** y;
  x == y;
  x != y;
  x === y;
  x !== y;
  x < y;
  x <= y;
  x > y;
  x >= y;
  x & y;
  x | y;
  x ^ y;
  x << y;
  x >> y;
  x >>> y;
  'x' in y;
  x instanceof y;
}